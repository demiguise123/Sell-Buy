<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pari.olxelem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FirstPage"></activity>
        <activity android:name=".Sell"></activity>
        <activity android:name=".BuyHomePage"></activity>
        <activity android:name=".Buy"></activity>
        <activity android:name=".MyAccount"></activity>
        <activity android:name=".Selling"></activity>
        <activity android:name=".SellingItem"></activity>

    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>